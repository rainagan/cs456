package a3;

import java.net.Socket;

public class fileExchangeThread extends Thread {
	private Socket up;
	private Socket down;
	public fileExchangeThread(Socket upload, Socket download) {
		this.up = upload;
		this.down = download;
	}
}
